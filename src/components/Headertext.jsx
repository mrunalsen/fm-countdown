import React from 'react';

const Headertext = ({ text }) => {
    return (
        <>
            <h1 className='text-white text-center tracking-[5px] text-2xl mt-auto'>{text}</h1>
        </>
    );
};

export default Headertext;